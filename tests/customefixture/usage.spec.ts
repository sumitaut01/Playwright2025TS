
import {test} from './customficture.spec';

test('should use custom fixture', async ({customFixture}) => {

    console.log(customFixture); // This will log: "This is a custom fixture value."

   // customFixture
});
